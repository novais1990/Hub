# Este projeto usa dois bots separados, cada um com seu próprio .env:
#
#   sales-bot/.env   → Bot de Vendas (veja sales-bot/.env.example)
#   payment-bot/.env → Bot de Pagamento (veja payment-bot/.env.example)
#
# Consulte o SETUP.md para instruções completas de configuração.
